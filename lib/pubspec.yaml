name: smart_clock_app
description: Smart Clock with Alarm and Smartwatch

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  shared_preferences: ^2.2.2
  flutter_blue_plus: ^1.31.0
  permission_handler: ^11.3.0
  intl: ^0.19.0

flutter:
  uses-material-design: true